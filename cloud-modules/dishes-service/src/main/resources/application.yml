server:
  port: 9001

spring:
  application:
    name: dishes-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: et2601
      config:
        namespace: et2601

  config:
    import: nacos:${spring.application.name}.yml




